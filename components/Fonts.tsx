import { Outfit, Anonymous_Pro, Hedvig_Letters_Serif } from "next/font/google";
 

export const outfit = Outfit({
  subsets: ["latin"],
  display: "swap",
});

export const anonymous = Anonymous_Pro({
  subsets: ["latin"],
  display: "swap",
  weight: "400"
});

export const hedvig = Hedvig_Letters_Serif({
  subsets: ["latin"],
  display: "swap",
});